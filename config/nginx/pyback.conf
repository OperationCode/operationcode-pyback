upstream pyback {
  ip_hash;
  server pyback:8000;
}

server {

    location /static/ {
        autoindex on;
        alias /src/static/;
    }

    location / {
        proxy_pass http://pyback/;
    }
    listen 8000;
    server_name localhost;
}